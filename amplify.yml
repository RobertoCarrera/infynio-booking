version: 1
frontend:
  phases:
    preBuild:
      commands:
        # Skip Chromium download from puppeteer to avoid long installs/timeouts
        - export PUPPETEER_SKIP_DOWNLOAD=1
        - npm ci --cache .npm --prefer-offline
    build:
      commands:
        - npm run build
  artifacts:
    baseDirectory: dist/mars-studio/browser
    files:
      - '**/*'
  cache:
    paths:
      - .npm/**/*
      - node_modules/**/*
